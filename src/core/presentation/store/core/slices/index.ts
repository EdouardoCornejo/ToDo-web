import authSlice from './auth/slice/auth.slice';
import todoSlice from './todo/slice/todo.slice'


export const coreSlicesPersist = {
    authSlice,
};

export const coreSlices = { 
    todoSlice,
};
